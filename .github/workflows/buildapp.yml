name: Build (Deprecated)

# The build workflow lives in the main YTLitePlus repository.
# See: YTLitePlus/.github/workflows/buildapp.yml

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: echo "This workflow has been moved to the main YTLitePlus repository."
