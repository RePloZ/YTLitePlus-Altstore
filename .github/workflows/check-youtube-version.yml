name: Check YouTube Version (Deprecated)

# This workflow has been moved to the main YTLitePlus repository.
# The version check now runs there and creates a GitHub Issue when
# a new YouTube version is detected on the App Store.
#
# See: YTLitePlus/.github/workflows/check-youtube-version.yml

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "This workflow has been moved to the main YTLitePlus repository."
          echo "The version check now runs there and creates a GitHub Issue"
          echo "when a new YouTube version is detected."
